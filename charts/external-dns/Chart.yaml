apiVersion: v2
name: external-dns-otc
description: ExternalDNS deployment for RKE2 on SwissOTC with Designate DNS provider
type: application
version: 0.1.0
appVersion: "0.15.0"

dependencies:
  - name: external-dns
    version: "1.15.x"
    repository: "https://kubernetes-sigs.github.io/external-dns/"
